Programming Style
1.1) It is a basic knowledge for programmer that 0 is false and 1 is true.
So it is bad to set in a oppsite way.

1.2) 
int isSmaller(char *first, char *second) {
	if (strcmp(first,second) < 1
		return 1;
	else 
		return 0;
}

1.3)
Didnt see any problem with this code 

1.4)
	a) if ( c != 'y' or c != 'Y')
		return;
	b) if (length > BUFSIZE) 
			length = BUFSIZE;
	c) flag = !flag
	d)	quote = (*line == '"')
	e) bit = val

1.5)
The order of input would not be notified, and it when show any instuction
for user.

1.6)
2 3
1 2

1.7)
	a) 
		if (!istty(stdin) && !istty(stdout) && !istty(stderr))
			return(0);
	b)
		return retval;
	c)
		for (k = 0; k<4;k++){
			scanf("%lf",&dx);
			x += dx;
		}

1.8)
It will not test count = 0. 
for ( int count = 0; count < total; count++){
	if  (this.getName(count) == nametable.userName()){
		return (true);
	}
}

1.9)
the parameter c occurs twice in the macro. 

1.10)
const float FTZMETER = 0.3048
const float METERZFT = 3.28084
const float MIZFT = 5280.0
const float MIZKM = 1.609344
const float SQMIZSQKM = 2.589988

1.11)
 a) comment the expected return value for the method
 b) comment for the explaination of the if condition
 c) comment for the method ability
 d) comment for the exactly act for the method
 e) comment for the explaination of next code. 

Python:
2.1) see s1.py
2.2) see s2.py
2.3) see id.py
2.4) see id2.py
2.5) see parsePhoneNrs.py

